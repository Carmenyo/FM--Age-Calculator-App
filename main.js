// Selecciona los elementos del DOM
    const input = document.querySelectorAll(".input");
    const label = document.querySelectorAll(".label small");
    const displayAge = document.querySelectorAll(".display span");
    const errorMessage = document.querySelectorAll(".error");
    const dayValue = document.querySelectorAll(".day");
    const monthValue = document.querySelectorAll(".month");
    const yearValue = document.querySelectorAll(".year");
    const button = document.querySelectorAll(".submit-button");
